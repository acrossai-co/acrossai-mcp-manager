# Memory Index

This is a compact routing map for durable memory. Keep it short. It points to source entries and helps agents decide what to read; it does not replace the source memory files.

## Active Decisions
| ID | Title | Scope | Tags | Status | Source |
|---|---|---|---|---|---|
| D1 | PLUGIN_NAME_SLUG uses literal string; get_plugin_name() returns property | Boot flow | constants, boot | Active | DECISIONS.md |
| D2 | Rewrite rules registered immediately at activation with placeholder vars | Activation | rewrite, routing | Active | DECISIONS.md |
| D3 | Compat.php placed in includes/ not includes/Utilities/ (boot-time shim) | Module structure | compat, placement | Active | DECISIONS.md |
| D4 | class_exists() guards in Activator are always silent no-op | Activation | db, class-loading | Active [scope-narrowed F011] | DECISIONS.md |
| D5 | PHPCS baseline exceptions in phpcs.xml.dist (filename, $_instance, docblocks, namespace Public, PSR12 header) | Plugin-wide | phpcs, baseline | Active | DECISIONS.md |
| D6 | Activator.php MUST use `use` imports (not bare/inline FQN) for all DB class references | Activation | namespace, fqn | Active | DECISIONS.md |
| D7 | Activator does NOT call insert_default_server() — Phase 4 MCPServerQuery::maybe_create_table() internal | Activation | db, seeding | Superseded (F011) | DECISIONS.md |
| D8 | AccessControl stub targets wpboilerplate/wpb-access-control ^2.0.0 vendor package FQN (bumped by F015 — see DEC-ACCESS-CONTROL-V2-ADOPTION) | Phase 7 prep | access-control, vendor, v2 | Active | DECISIONS.md |
| D9 | BerlinDB-style Query interface (Schema/Table/Row/Query) hand-rolled — no berlindb/core vendor dep | Database layer | berlindb, query, vendor | Superseded (F011) | DECISIONS.md |
| D10 | Minimal-port deferral pattern — partial port + reserved follow-up task when source class depends on un-ported siblings | Migration | port, deferral, scope | Active | DECISIONS.md |
| D11 | Phase X.0 absorption — when a phase's P0 prereq doesn't yet exist, absorb its setup as a sub-phase in the consuming phase, not a separate dedicated phase | Process | phase, prereq, p0-gate | Active | DECISIONS.md |
| D12 | Bulk task-status updates MUST be followed by a re-audit of environment-dependent gates | Process | tasks, completion, ci-gates | Active | DECISIONS.md |
| D13 | Constitution-level formalization vs. Accepted Deviation — escalate to `.specify/memory/constitution.md` when the deviation describes a generalizable pattern (≥2 features or forward-looking); reserve INDEX.md `DEV*` rows for one-off carve-outs | Process | constitution, deviation, governance, generalizable | Active | DECISIONS.md |
| D14 | Cross-phase state observation via public-static predicate on the owning module — consumer uses `use` import, never duplicates internal magic strings; matches A11 + B11 defensive-read families | Cross-feature interface design | cross-phase, predicate, static, A11, S9-adjacent | Active | DECISIONS.md |
| D15 | Shared package bootstrap in plugin entry file — scoped A1 deviation for vendor packages owning cross-plugin resources; gated by `did_action('<resource>_bootstrapped')` idempotency + `class_exists()` defense-in-depth; established by Features 038 + 010 across the AcrossAI codebase family | Cross-plugin coordination | a1-deviation, shared-package, bootstrap, plugins_loaded, generalizable | Active | DECISIONS.md |
| D16 | Template-method helpers pre-plan an optional override for the value most likely to vary — prevents raw-call bypasses that trip DRY grep gates (F013 nonce_field escape hatch) | Helper design | template-method, dry, override, grep-gate | Active (F013) | DECISIONS.md |
| D17 | A1 hook-registration by transitivity — Loader-wired bootstrap methods inherit A1 conformance for their inner add_shortcode/add_action/add_filter calls; codified by F013 ClientRendererController::register_shortcodes_and_actions | Hook wiring | a1, transitivity, bootstrap, shortcodes | Active (F013) | DECISIONS.md |
| DEC-BERLINDB-TABLE-REQUEST-BOOT | BerlinDB Table subclasses MUST be instantiated at request time via `Main::load_hooks()` — activation-time `Table::instance()` alone leaves BerlinDB's DB interface empty on subsequent requests, causing Query to fall back to `$table_alias` as FROM | BerlinDB, request lifecycle | berlindb, boot, request-lifecycle, main-php, generalizable | Active (F011) | DECISIONS.md |
| DEC-BERLINDB-SUBCLASS-NO-USE-COLLISION | Do NOT `use BerlinDB\Database\Kern\<X>` when the subclass name matches the parent's name — the `use` claims the same local symbol and produces "Cannot redeclare class" fatals. Drop the `use` and extend via leading-`\` FQN, or alias the import | BerlinDB, namespace | berlindb, namespace, class-collision, subclass-naming, workflow-template | Active (F011) | DECISIONS.md |
| DEC-VENDOR-SETTINGS-TAB-INTEGRATION | Canonical shape for adding a tab to `acrossai-co/main-menu`'s shared Settings page — filter `acrossai_settings_tabs`, `SettingsPage::tab_page_slug()` helper, SHARED `'acrossai-settings'` option group (NOT the per-tab page slug), sibling-style class member ordering, no `class_exists()` guard on the vendor call; accepted §IV DataForm carve-out | Cross-plugin vendor integration | vendor-integration, settings-api, main-menu, dataform-carveout, class-exists-omission | Active (F012) | DECISIONS.md |
| DEC-UNINSTALL-OPT-IN-GATE | `uninstall.php` MUST short-circuit at the top when `acrossai_mcp_uninstall_delete_data !== 1`; preserve-by-default satisfies WP.org guideline #5; every destructive SQL statement MUST live after the gate; default value MUST be `0` | Uninstall path | uninstall, safety-invariant, wp-org-guideline-5, opt-in, behavior-change | Active (F012) | DECISIONS.md |
| DEC-ADMIN-SURFACE-PRUNE-CLI-AUTH-LOG | Standalone admin submenus for read-only DB-inspection views SHOULD be pruned when a lighter path exists (WP-CLI, per-server tab); first A9 subtractive-edit precedent — submenu removal + `plugin_screen_ids()` entry removal MUST land in the same feature; narrows DEV1 (WP_List_Table exception) to shared pages + dashboard widgets, not dedicated submenus | Admin surface | admin-surface, pruning, a9-subtractive-precedent, dev1-scope-narrowing, list-table | Active (F012) | DECISIONS.md |
| DEC-SERVER-TAB-CLASS-HIERARCHY | Template-method + Registry pattern for multi-tab admin surfaces — AbstractServerTab base with abstract slug/label/render_body + shared HTML helpers, Registry singleton for dispatch, final concrete tab classes with opt-in visible_for override. Canonical for any admin surface with 3+ tabs on a per-record edit page | Admin partials, multi-tab pages | template-method, registry, singleton, admin-partials, dry | Active (F013) | DECISIONS.md |
| DEC-CLIENT-RENDERER-PUBLIC-API | Public Renderer layer under public/Renderers/ for cross-context MCP client-config UI reuse. 4 sanctioned entry points: static render(), acrossai_mcp_render_client_block action hook, acrossai_mcp_client_block_context filter, 3 shortcodes. Plus acrossai_mcp_client_classes filter for MCPClientsBlock sub-nav extension. Security invariants: cap check via context.cap (never hardcoded manage_options), App Password locked to get_current_user_id() at both UI + REST layers, cross-context nonce action binding (server_id + context slug), F012 toggle enforcement inside Renderer. `@experimental` until 1.0.0 tag. Accepted §IV DataForm carve-out (JSON display + WordPress-core App Password creation, not data-entry form). **Post-F016 (2026-07-07): dispatch map shrinks to 2 entries (`npm`, `clients`) + 2 shortcodes (`acrossai_mcp_npm_block`, `acrossai_mcp_clients_block`); ClaudeConnectorBlock retired. `@experimental` allowance covers the surface reduction.** | Public API, cross-plugin integration | public-api, renderer, cross-context, experimental, shortcode, security-critical | Active (F013; annotated F016) | DECISIONS.md |
| DEC-ACCESS-CONTROL-V2-ADOPTION | Canonical v2 wrapper pattern for AcrossAI-family plugin consumption of wpb-access-control ^2.0.0: PROVIDERS_FILTER + TABLE_SLUG constants, is_available/boot_manager/get_manager/register_rest_api/maybe_show_library_notice methods, gate_mcp_tool_call filter callback, register_default_providers static. Activator MUST call `(new RuleTable(TABLE_SLUG))->maybe_upgrade()` behind class_exists guard (SEC-015-001). Uninstall opt-in gate MUST purge namespace + drop table + delete version option. Fail-open when package absent (sibling DEC-PERM-CB). Single-provider dropdown UX (Q4 supersedes Q1 SAFE_CAPABILITIES allow-list — admins bypass every rule per v2 hierarchy, so full capability exposure is safe). Supersedes D8's `^1.0` version pin in-place (not deprecation) | Access control adoption | access-control, v2-adoption, wrapper, table-slug, fail-open, mcp-boundary, single-provider-picker | Active (F015) | DECISIONS.md |
| D18 | `mcp_adapter_pre_tool_call` filter is the canonical MCP-boundary enforcement hook — return WP_Error with array('status'=>403) to short-circuit. Do NOT fork mcp-adapter, do NOT hook ability-level permission_callback. Signature: apply_filters( 'mcp_adapter_pre_tool_call', $args, $tool_name, $mcp_tool, $server ). Fired by vendor/wordpress/mcp-adapter/includes/Handlers/Tools/ToolsHandler.php:182. Loader-wire via Main::define_public_hooks() per A1 | Hook wiring, cross-cutting enforcement | mcp-adapter, enforcement-hook, filter, cross-cutting | Active (F015) | DECISIONS.md |
| D19 | Fail-open observability pattern — on defensive fail-open paths (missing server, unavailable vendor, invalid provider), fire a scoped `do_action()` so operators can log the anomaly via any observability tool without a hard dependency. Fire-and-forget; return value ignored. Layered: plugin-scoped hooks sit alongside vendor-provided lower-level hooks. F015 codifies via `acrossai_mcp_access_control_denied` (fires BEFORE WP_Error/empty-list return) + `acrossai_mcp_access_control_missing_server` (fires on Q2 race) | Observability, defensive programming | fail-open, observability, do_action, defensive-programming | Active (F015) | DECISIONS.md |
| D20 | Before wiring a consumer-side default-provider-registration filter, verify the vendor's own default set doesn't already cover it — otherwise you double-register. F015 skipped `add_filter(PROVIDERS_FILTER, register_default_providers)` because wpb-access-control v2 internally registers 5 defaults (WpRole/WpUser/WpCapability + BuddyBoss + MemberPress) via load_providers() | Consumer plugins of shared vendor packages | vendor-integration, default-registration, filter-wiring, double-registration, redundancy | Active (F015) | DECISIONS.md |
| D21 | Fresh-install-only retirement pattern: when operator attestation covers no-live-data OR operator explicitly manages migration, retirement features MUST NOT ship in-plugin schema-migration code (no idempotent DROP in Activator, no `$version` bump, no fallback helper). Operator runs the manual SQL recipe from README.txt §Unreleased and reactivates. `uninstall.php` keeps defunct entries AFTER opt-in gate as idempotent safety net. Reduces diff size, eliminates migration test surface, removes Activator destructive-SQL risk. Reject when public wp.org ship (no attestation) OR retention-constrained data — apply F011 self-healing pattern instead | Retirement / feature-retirement subtractive-edit PRs | fresh-install-only, retirement, subtractive-edit, operator-managed-migration, generalizable | Active (F016) | DECISIONS.md |
| DEC-ABILITY-OVERRIDE-RESOLUTION | Effective ability exposure per `(server, ability)` has a single resolver — row in `acrossai_mcp_server_abilities` wins, else `meta[mcp][public]`. Every consumer (REST GET, REST POST audit, `mcp_adapter_pre_tool_call` gate, future WP-CLI) MUST route through `ExposureResolver::resolve()` — do NOT re-derive the fallback rule | Ability exposure resolution, per-server overrides | resolver, single-source-of-truth, fallback, per-request-cache, generalizable | Active (F017) | DECISIONS.md |
| DEC-WP-DATAVIEWS-OVER-REACT | New admin JS surfaces MUST use `@wordpress/dataviews` + `@wordpress/components` + `@wordpress/element` + `@wordpress/api-fetch` + `@wordpress/i18n` + `@wordpress/hooks`. Generic React libraries (`react-query`, `@tanstack/*`, `redux`, `mobx`, `react-table`, `@mui/`, `styled-components`) are FORBIDDEN. Grep gate on `src/js/` enforces at review | Admin UI, Principle IV+VI implementation | dataviews, wp-packages-first, react-libs-forbidden, principle-iv, principle-vi | Active (F017) | DECISIONS.md |
| DEC-TOOL-SELECTION-PRESENCE-MODEL | When UI models set-membership with exactly two states, storage MUST be presence-based (row exists ⇔ true; no row ⇔ false), NOT a boolean column that would introduce a phantom third state. Enforce with `UNIQUE(identifying_tuple)`. Contrast with F017's boolean-with-resolver pattern (needed when a fallback layer exists). Decision rule: model third-state presence in UI first; if boolean `false` is indistinguishable from "no row" in UI, drop the boolean | Feature data-model design (per-resource config surfaces) | presence-based-storage, boolean-antipattern, ui-driven-data-model, set-membership, third-state-avoidance, generalizable | Active (F020) | DECISIONS.md |
| DEC-F020-TOOL-ENFORCEMENT-PRIORITY | `mcp_adapter_pre_tool_call` priority slot map: **10 = F015 access control, 20 = F017 ability exposure, 30 = F020 tool curation**. New gates MUST occupy slot 40+. Deny-precedence honored — never re-allow an earlier `WP_Error`. Consider extracting `McpAdapterGateRegistry` at 4th gate. Annotates D18's canonical filter contract | Enforcement gate stacking, feature wiring in `Main.php` | mcp-adapter-pre-tool-call, priority-slot-map, deny-precedence, gate-stacking, f015-f017-f020, generalizable, d18-annotation | Active (F020) | DECISIONS.md |
| D22 | Inline-shipped follow-up features MUST fold into parent spec as concrete task IDs (`T<X>-<seq>` pattern), not "See F###" pointers — pointer breaks reviewer traceability + lets boundary gates miss newly-added files. Preserve child spec dir for history; enumerate every child file under a parent-phase heading | Spec-Kit tasks.md hygiene, cross-feature tracking | tasks-ledger, fold-in, traceability, inline-shipping, spec-kit, generalizable | Active (F021 Phase 10 / F024) | DECISIONS.md |
| DEC-ADDONS-PAGE-VENDOR-CTOR-BOOT | External-package classes whose constructor self-registers all WordPress hooks (e.g. `\AcrossAI_Addon\AddonsPage` in `acrossai-co/main-menu`) are exempt from A1's Loader-only rule. Instantiate once in `Main::define_admin_hooks()` under a `class_exists` guard + `try/catch` with an admin-notice fallback gated on `manage_options`. Freemius credentials stored inline (no filter/option indirection) | Vendor integration, admin-menu wiring | vendor-integration, main-menu, addons-page, external-package, a1-exception, boot-flow, class-exists-guard, try-catch, generalizable | Superseded (F028) | DECISIONS.md |
| DEC-FREEMIUS-DOUBLE-OPTIN-GATES-ACCOUNT | Freemius' Account/Add-ons/Contact submenus require `is_registered() = true` which requires `sites`+`users` entries at top level of `wp_options.fs_accounts`. For `is_wp_org_compliant: true` products (AcrossAI umbrella default), that requires a completed email round-trip: "Allow & Continue" click only sets `is_pending_activation: true`; user MUST click the confirmation link Freemius emails to `wp_users.user_email` (NOT `wp_options.admin_email`). Sanity SQL: `SELECT option_value LIKE '%"sites"%' AS has_sites, option_value LIKE '%"users"%' AS has_users FROM wp_options WHERE option_name = 'fs_accounts';`. LocalWP `.local` sites need `WP_FS__DEV_MODE` + `WP_FS__API_HTTP_INSECURE` defined in `wp-config.php` before first opt-in | Freemius opt-in state machine, submenu visibility diagnosis | freemius, double-opt-in, wp-org-compliant, is_registered, is_pending_activation, fs_accounts, localwp, generalizable | Superseded (F028) | DECISIONS.md |
| DEC-F025-HYBRID-TOOL-STORAGE-PROTOCOL-VS-CURATED | Hybrid storage for fixed-cardinality + open-ended set — boolean columns + presence rows unioned via single canonical composer; supplements DEC-TOOL-SELECTION-PRESENCE-MODEL under its own boolean-with-fallback carve-out (F017 ExposureResolver was first use, F025 is second) | Feature data-model design | hybrid-storage, boolean-columns, presence-rows, canonical-composer, dec-supplement, f017-lineage, generalizable | Active (F025) | DECISIONS.md |
| DEC-F025-V2-VENDOR-SOURCE-CROSS-CHECK-CADENCE | For vendor hook/return contract consumers: EITHER plan.md cites vendor-source lines OR v2 security review is mandatory. Companion DEC-F025-RUNTIME-EVIDENCE-OVER-STATIC-REVIEW: v2 must flag hook-timing-safety claims as "requires runtime verification" when static analysis is insufficient. Corollary: prefer plugin-owned constants over runtime vendor resolution | Reviewer cadence, vendor integration | vendor-integration, cross-check, security-review-v2, runtime-evidence, canonical-constants, generalizable | Active (F025) | DECISIONS.md |
| DEC-F025-TASKS-REVIEW-PRESERVATION-INVARIANT-AND-COVERAGE-MATRIX | Subtractive-edit tasks on security-sensitive methods MUST list preserved invariants (permission_callback, nonce, capability checks); task-reviews MUST produce a coverage matrix mapping each plan-review finding to its remediation task ID | Task authoring + review artifacts | task-authoring, subtractive-edit, preservation-invariant, coverage-matrix, review-artifact, extends-d6, generalizable | Active (F025) | DECISIONS.md |
| D23 / DEC-F026-SIBLING-COMPOSER-EXTENSION-PATTERN | When extending a canonical composer, add a sibling — at the method level if the new semantic fits the same class (F026 v1: `compose_effective_tools_for_row` alongside `compose_for_row`), at the class level if the scope outgrows the class's naming (F026 v2: `AbilityDiscovery` alongside `ToolPolicy`). Never overload with a flag argument; never rename to broaden. Preserves call-site clarity + zero behavior drift on consumers of the original | Class/method extension pattern | sibling-composer, extension, no-overload, no-rename, canonical-composer, f026, generalizable | Active (F026) | DECISIONS.md |
| D24 / DEC-F026-ADVERTISEMENT-VS-CALL-TIME-DEFENSE-IN-DEPTH | Advertisement-time enforcement (post-F026 v3: inside plugin-owned meta-tool callbacks via `AbilityHelpers::apply_exposure_filter` → `ExposureResolver::resolve`) and call-time gate (`AbilityExposureGate` @ `mcp_adapter_pre_tool_call` priority 20) are INDEPENDENT enforcement layers. Never drop either layer; never route ability-exposure decisions through a path that skips `ExposureResolver`. **New corollary (v3)**: exposure ≠ authorization — companion filters widening exposure MUST NOT bypass the target ability's own `permission_callback`, enforced by `Execute::check_permission` ordering | F017/F025/F026 threat model, enforcement layering | defense-in-depth, advertisement-vs-call-time, ability-exposure-gate, exposure-resolver, exposure-neq-authorization, f017-f025-f026, generalizable | Active (F026 v3) | DECISIONS.md |
| D25 / DEC-F026-WP-REGISTER-ABILITY-ARGS-CALLBACK-SWAP | To override vendor-registered ability behavior (keep schema, replace callbacks), hook WP core `wp_register_ability_args` and rebind `execute_callback` + `permission_callback` on the target slugs. Rejected alternatives: unregister+re-register (schema-drift + LOC bloat), priority-race registration (`_doing_it_wrong` noise), disable vendor server, post-hoc intercept (metadata-enumeration hazard). Reference impl: `includes/Abilities/CallbackReplacer.php`. Companion pattern: `A17` (request-scoped REST context capture — required because swapped callbacks don't receive McpServer context) | Vendor-ability override pattern | vendor-integration, wp-register-ability-args, callback-swap, abilities-api, mcp-adapter, f026, generalizable | Active (F026 v3) | DECISIONS.md |
| D26 / DEC-CONSUMER-SELF-EXCLUSION-VIA-VENDOR-FILTER | Consumer plugins listed in a vendor's hardcoded ecosystem-list baseline (e.g. `AddonsPageRenderer::ADDONS`) MUST ship a small `admin/Partials/<X>Filter` singleton that hooks the vendor's `apply_filters()` and drops their own slug. Prevents self-referential "Install" cards on the shared Add-ons page. Complements D20 (additive) on the subtractive side. Requirements: singleton, Loader-wired, reuses plugin's canonical slug constant (D1), defensive normalization + array_values reindex. Reference impl: `admin/Partials/AddonsFilter.php` (60 LOC) | Cross-plugin vendor integration | vendor-integration, main-menu, consumer-self-exclusion, addons-page, filter-driven-list, admin-partials, generalizable | Active (F028) | DECISIONS.md |
| D27 / DEC-CONFIDENTIAL-CLIENT-SOFT-AUTH-VIA-PKCE | When a `client_secret_post`-registered client submits NO secret at `/token` (header + body both empty), fall through to PKCE-only verification for `authorization_code` / refresh-token-bound-to-client verification for `refresh_token` instead of rejecting with `invalid_client`. Applies to both grant handlers symmetrically. Clients that DO submit a secret are still verified via `ClientRepository::verify_secret` (unchanged). Residual risk bounded by mandatory PKCE S256 + RFC 8707 audience + single-use codes + refresh-family revocation. Complements F027's DCR default fix. Reference impl: `TokenController.php:106-111` and `TokenController.php:203-208` | OAuth /token defense-in-depth | oauth-2-1, client-secret-post, pkce-only, defense-in-depth, mcp-host-interop, security-tradeoff, generalizable | Active (F029) | DECISIONS.md |
| D28 / DEC-BERLINDB-SCHEMA-DRIFT-RECONCILIATION | Every change to a BerlinDB Kern\Table's Schema.php on a table that live installs already have MUST ship as 3 coordinated edits: (1) bump `$version` in the paired Table.php, (2) register a `$upgrades = ['<v>' => 'upgrade_to_<v>']` callback that runs idempotent ALTER via `INFORMATION_SCHEMA` existence/width check + returns bool, (3) ensure `maybe_upgrade()` fires on `admin_init` (not just Activator) via `Main::reconcile_database_schemas()` at priority 3. BerlinDB `upgrade()` only walks `$this->upgrades` — bare `$version` bump with empty `$upgrades` silently stamps the new version and touches nothing. dbDelta is NOT invoked. Reference impls: `CliAuthLog\Table::upgrade_to_1_0_1` (MODIFY) + `MCPServer\Table::upgrade_to_1_1_1` (ADD COLUMN) | BerlinDB schema evolution | berlindb, schema-drift, upgrades-callback, maybe-upgrade, admin-init, generalizable | Active (F029) | DECISIONS.md |
| D29 / DEC-F030-PERMISSION-CALLBACK-OPERATOR-OPT-IN-BYPASS | Operator-visible `permission_callback` bypass permitted ONLY with all six defensive layers: (1) `manage_options` cap re-verified inside handler, (2) per-server nonce bound to server_id, (3) persistent warning banner when ON, (4) native `confirm()` on submit-to-ON, (5) `CurrentServerHolder` request scope (non-MCP callers unaffected), (6) `ExposureResolver::resolve()` junction-row gate. Under these six layers, hooking `wp_register_ability_args` at a priority higher than every other consumer (F030 P999999) and returning `true` is legitimate. Scoped carve-out from `D24` corollary v3 — does NOT generalize to any exposure-widening filter that lacks explicit operator opt-in. Reference impl: `PermissionOverrideProcessor.php`. | Ability permission enforcement, per-server operator overrides | wp-register-ability-args, permission-callback, operator-opt-in, six-layer-gating, d24-carve-out, generalizable | Active (F030) | DECISIONS.md |
| D30 / DEC-F030-EXPLICIT-EXPOSURE-ONLY | F030 intentionally passes empty `$meta` to `ExposureResolver::resolve( $server_id, $slug, array() )` to collapse the `meta.mcp.public` fallback path → operator-opt-in bypass ONLY applies to abilities with an EXPLICIT junction row in `wp_acrossai_mcp_server_abilities`. `meta.mcp.public = true` abilities WITHOUT a junction row are NOT bypassed even when the override flag is ON. Scoped carve-out from `DEC-ABILITY-OVERRIDE-RESOLUTION` — the narrower scope keeps `D29` layer #6 meaningful (operator explicitly toggled this ability ON for this server, not "operator toggled the server ON and this ability happens to be public"). Reference: 20-line inline comment at `PermissionOverrideProcessor.php:123-142`. | Ability exposure resolution, per-server security bypass gating | exposure-resolver, empty-meta, explicit-opt-in, dec-ability-override-resolution-carve-out, security-conservatism, f030 | Active (F030) | DECISIONS.md |

## Architecture Constraints
| ID | Constraint | Scope | Tags | Source |
|---|---|---|---|---|
| A1 | All hook registration lives exclusively in `includes/Main.php` via `define_admin_hooks()` / `define_public_hooks()` | Plugin-wide | boot, hooks | ARCHITECTURE.md |
| A2 | Every feature class uses the singleton `instance()` pattern | Plugin-wide | singleton, DI | ARCHITECTURE.md |
| A3 | All admin UI classes live in `admin/Partials/` with namespace `AcrossAI_MCP_Manager\Admin\Partials` | Admin | namespace, structure | ARCHITECTURE.md |
| A4 | All new data forms use `DataForm` from `@wordpress/dataviews`; all new lists use `DataViews` | Admin UI | UI, components | ARCHITECTURE.md |
| A5 | MCP server listing MUST use `wpboilerplate/wpb-mcp-servers-list` package, not direct adapter calls | MCP module | integration | ARCHITECTURE.md |
| A6 | Any class in namespace `AcrossAI_MCP_Manager\\Includes` MUST use `use` imports or FQN with leading `\\` when referencing sub-namespace classes — bare relative names silently fail | Plugin-wide | namespace, fqn | ARCHITECTURE.md |
| A7 | All 6 plugin constants defined exclusively in Main::define_constants() via private define() guard — zero define() calls elsewhere | Plugin-wide | constants, boot | ARCHITECTURE.md |
| A8 | Access control wiring MUST use \WPBoilerplate\AccessControl\AccessControlManager vendor package (v2.0.0+ via wrapper class per DEC-ACCESS-CONTROL-V2-ADOPTION) | Plugin-wide | access-control, vendor, v2, wrapper | ARCHITECTURE.md |
| A9 | Shared admin constants (read by ≥2 modules) live in includes/Utilities/ as a final class, NOT on sibling feature classes | Plugin-wide | constants, coupling, utilities | ARCHITECTURE.md |
| A10 | WP_List_Table subclasses are exempted from the singleton-only rule — public ctor required by parent; instantiated per-render; never Loader-wired | Admin | list-table, singleton, exception | ARCHITECTURE.md |
| A11 | Pure service classes (stateless value producers, e.g. includes/MCPClients/) exempted from singleton rule — no instance state, no ctor args, no hook registration | Plugin-wide | singleton, service-class, exception, pure | ARCHITECTURE.md |
| A12 | Pure-PHP modules claiming WP-independence MUST have a tests/bootstrap.php that loads ONLY composer autoload — the test harness is the proof of the architectural claim | Plugin-wide | test-harness, purity, bootstrap, wp-free | ARCHITECTURE.md |
| A13 | RFC-prescribed forms (e.g. OAuth consent) are exempted from A4 DataForm mandate — cite the RFC section in the rendering class docblock. **Post-F016 (2026-07-07): NO active consumers — retired OAuth consent form was the sole implementer. Constraint remains valid for any future RFC-prescribed form (device-grant consent, PKCE authorize surfaces, etc.) but currently has zero consumers.** | Admin UI | data-form, rfc, oauth, exception, no-active-consumers-f016 | ARCHITECTURE.md |
| A14 | WP-CLI dispatch classes follow A11-style exemption — stateless, no singleton, instantiated via `new self()` inside `\WP_CLI::add_command()` | Plugin-wide | wp-cli, singleton, exception, pure | ARCHITECTURE.md |
| A15 | Database-namespace audit-recorder static helpers (e.g. `<Module>\Recorder`) follow A11/A14 family — stateless static methods wrap `Query::add_item` for audit writes inside `try/catch` | Database, audit | recorder, audit, singleton, exception, pure | ARCHITECTURE.md |
| A16 | Every AcrossAI plugin consuming an `acrossai-co/*` vendor package MUST declare an explicit `repositories` VCS entry in `composer.json` pointing at the GitHub URL. Prevents Packagist-sync-lag failures on `composer update` immediately after a new upstream tag is pushed | Plugin-wide (composer configuration) | composer, vendor, vcs-repository, packagist-lag, acrossai-co, generalizable | ARCHITECTURE.md |
| A17 | Request-scoped WP REST context capture pattern: when a plugin callback runs during REST dispatch but does NOT receive `WP_REST_Request` (e.g., WP core Abilities API `execute_callback` bound via `wp_register_ability_args`), capture context in a request-scoped singleton via 3 hooks — `rest_pre_dispatch` @ priority 5 (populate), `rest_post_dispatch` @ 999 (normal-path clear), `shutdown` @ 999 (**safety-net clear** for long-lived PHP processes where fatal errors kill the request before rest_post_dispatch). Missing the shutdown binding is the #1 silent bug in ad-hoc versions | Cross-feature REST integration | rest-pre-dispatch, request-scoped-singleton, shutdown-safety-net, roadrunner, frankenphp, abilities-api, f026, generalizable | Active (F026 v3) | ARCHITECTURE.md |

## Bug Patterns
| ID | Pattern | Affected Area | Tags | Source |
|---|---|---|---|---|
| B1 | Namespace relative-path bug: bare `Includes\\Database\\...` inside `AcrossAI_MCP_Manager\\Includes` → double-Includes FQN → class_exists always false | Activator, any Includes class | namespace, silent-failure | BUGS.md |
| B2 | define_constants() null-property: using `$this->property` before it is set in constructor → constant defined as empty string | Main.php boot | constants, null | BUGS.md |
| B3 | TODO stub FQN namespace drift: missing \Includes\ segment → fatal on uncomment | Main.php stubs | namespace, stub | BUGS.md |
| B4 | Unescaped dot in add_rewrite_rule() PCRE pattern: '^.well-known/' matches any char; must be '^\.well-known/' | Activator | rewrite, regex | BUGS.md |
| B5 | Public constructor on singleton allows duplicate instantiation → double hook registration | Any singleton class | singleton, hooks | BUGS.md |
| B6 | admin_url() without esc_url() → filterable value injected into HTML href → XSS | Admin Partials | xss, escaping | BUGS.md |
| B7 | Mass-assignment via forged POST keys to $wpdb->update/insert — Query writers MUST filter against Schema::columns() before persisting | Custom DB tables | mass-assignment, query | BUGS.md |
| B8 | "// esc_url'd above" comments don't enforce escaping — re-escape at output point even if redundant (esc_* is idempotent) | Admin Partials renders | xss, escaping, defense-in-depth | BUGS.md |
| B9 | PHPUnit 13+ silently ignores `@dataProvider` annotations — use `#[DataProvider]` PHP attribute instead. Same for `@depends`, `@group`, `@test` | PHPUnit tests | testing, phpunit, attributes | BUGS.md |
| B10 | Check-then-act on one-shot credentials under concurrency → use atomic single-statement CAS (`UPDATE ... WHERE id = :id AND completed_at IS NULL`), not `SELECT` + `UPDATE` | OAuth, custom DB writes | concurrency, race, atomic-cas, one-shot | BUGS.md |
| B11 | Transient-stored associative arrays MUST be defensively validated with `is_array() + isset() + is_numeric()` triple-check on read — defends against partial writes, type drift, transient corruption | Transient readers, OAuth, CLI auth | transient, defensive, validation, partial-write | BUGS.md |
| B12 | wp_enqueue_scripts doesn't fire when template_redirect exits before wp_head() — call the enqueue method explicitly from the render helper, not only via Loader hook | template_redirect handlers, standalone HTML pages | enqueue, template_redirect, wp_head, silent-failure | BUGS.md |
| B13 | wp_redirect filter MUST throw to intercept the trailing exit in tests — returning false cancels the header but the test runner still dies on exit | PHPUnit tests of state-mutation redirects | testing, wp_redirect, wp_safe_redirect, exit, filter | BUGS.md |
| B14 | register_activation_hook default priority 10 fatals before higher-priority-number guards can run — vendor autoload checks belong at priority 1 to wp_die() gracefully before the main activation callback runs | Plugin activation, vendor autoload | activation, register_activation_hook, priority, silent-failure | BUGS.md |
| B15 | Regex verification gates that pattern-match only the bare-name form silently pass while missing the leading-`\` FQN form (`extends \Foo`) and short-name aliased form (`new Query()` via `use ...\Query;`) — use ERE with `\\?` optional-backslash or run two grep passes | Verification gates, code review | grep, regex, verification, gate-hygiene, false-negative, refactor, sweep | BUGS.md |
| B16 | Mixed positional (`%s`) + numbered (`%1$s`/`%2$s`) placeholders in a single printf silently mislabel output — the numbered placeholders bind to args 1..N (the leading text args), not the URLs/labels appended after. Split into two calls, or convert everything to numbered form | Admin partials that emit translated HTML with wp_kses_post() | printf, format-string, placeholder, i18n, visual-qa, silent-failure, xss-adjacent | BUGS.md |
| B17 | `rest_url()` returns URL WITH trailing slash — consumers that concatenate sub-paths produce `//`-double-slash URLs that WordPress doesn't route → 404. Strip via `untrailingslashit(rest_url())` at the boundary, OR use `rest_url('sub-path')` so WordPress joins correctly. Applies to any third-party JS bundle / config / template that builds URLs by concat | wp_localize_script, vendor React apps, REST URL builders | rest-url, trailing-slash, concat, 404, silent-failure, wp-localize-script | BUGS.md |
| B18 | `$wpdb` returns MySQL TINYINT columns as string — `1 === $row->col` is always false. Cast to `(int)` before strict compare, or use `! empty()` for boolean semantics. BerlinDB Row property-type declarations (`public int $col`) are documentation, not runtime typing — the driver still returns strings | BerlinDB Row consumers, list-table row-map, toggle handlers | wpdb, tinyint, strict-compare, berlindb, row, silent-failure | BUGS.md |
| B19 | WP Application Password client-config generators MUST emit BOTH `WP_API_USERNAME` (from `wp_get_current_user()->user_login`) and `WP_API_PASSWORD` — HTTP Basic needs both to build `Authorization: Basic base64(user:pass)`. Password-only configs silently 401 on every request from the consuming app | MCP/HTTP client config generators, App Password consumers | wp-application-password, http-basic, config-generator, silent-failure, auth | BUGS.md |
| B20 | Plaintext OAuth secret in `varchar(255)` column is an S3 constitution violation — OAuth secrets/tokens MUST be `char(64)` (SHA-256 hashed). Grep gate: `'name' *=> *'[^']*_(secret\|token\|password\|key)'` in Schema.php files MUST have `'type' => 'char'` length >= 64. F016 retroactively closed for `claude_connector_client_secret`. Retroactive fix pattern: (1) delete write path, (2) pre-DROP UPDATE to overwrite InnoDB pages, (3) ALTER TABLE DROP COLUMN, (4) prune Schema/Row/Seeder | Custom DB tables, admin form handlers writing OAuth secrets | s3, plaintext-secret, wpdb, berlindb, retroactive-fix, data-at-rest | BUGS.md |
| B21 | BerlinDB v3 `Column` recognized flags do NOT include `date_updated` — the auto-update-on-write timestamp flag is spelled `modified` (see `vendor/berlindb/core/src/Database/Kern/Column.php` @param docblock). Passing an unrecognized flag creates a dynamic property, which trips the PHP 8.2+ "Creation of dynamic property Column::$X is deprecated" notice at every column boot. Grep gate for new BerlinDB Schemas: `grep -rn "'date_updated'" includes/Database/` MUST return zero matches. Recognized flags for datetime columns: `created` (INSERT-time stamp), `modified` (UPDATE-time stamp), `date_query` (enables __between / __compare) | Custom DB tables, BerlinDB Schema authors | berlindb-v3, dynamic-property, php82, deprecation, schema-flags, silent-notice | BUGS.md |
| B22 | New `@wordpress/*` packages (v0.x) not yet in `@wordpress/scripts` externals map need runtime string store lookup (`wp.data.select('core/foo')` + REST fallback), not build-time import. Build-time import silently bundles or manifest-lists under an unregistered handle → runtime store lookup returns undefined → React app boots but never populates. Recovery: string-key + fallback state. Grep gate: `import.*from '@wordpress/(?!scripts\|env)'` needs review | Client JS bootstrap, admin React entries | wordpress-scripts, externals, runtime-lookup, silent-failure, boot, react | BUGS.md |
| B23 | Test-suffix method names (`_reset_cache_for_tests()`, `_for_testing()`) called from production code path are a silent-regression smell — maintainer removes the method during a cleanup sweep, invariant enforced by the call site silently breaks (missed action fires, stale cache reads). Prefer production-shape names (`clear_request_cache`) or redesign to eliminate the dependency. Grep gate: `_reset.*for_tests\|_for_testing\|_test_only` in `includes/`+`src/` needs per-hit classification | Static / singleton state reset, per-request cache invalidation, hook-fire invariants | naming, test-hook, silent-regression, cache-invalidation, refactor-hazard | BUGS.md |
| B24 | Vendor accessor assumption via `instanceof` silently fails when vendor namespace differs from the assumption — F020's `\WP\MCP\Server` guess was wrong (real class `\WP\MCP\Core\McpServer` with `get_server_id(): string` returning a slug). Use `method_exists( $obj, 'accessor_name' )` duck-typed feature detection; canonical implementations at F015/F017/F020 enforcement gates. Grep gate: `instanceof.*\\.*McpServer\|->get_id\(\)` MUST be zero OR nested under a `method_exists` check | Cross-package integration (mcp-adapter payloads, WordPress action/filter callbacks with vendor objects) | vendor-accessor, method-exists, instanceof-antipattern, silent-failure, enforcement-gate, sec-020-007, generalizable | BUGS.md |
| B25 | WordPress admin JS bundles enqueued on plugin screens inherit `wpApiSettings.root` from WordPress core — explicit `apiFetch.use( apiFetch.createRootURLMiddleware( config.rootURL + '/' ) )` is redundant AND risks silent 404 (trailing-`/` base + leading-`/` path → `//`-doubled URL). Wire ONLY `createNonceMiddleware` in admin context; let core handle URL rooting (matches F017 abilities.js:95). Grep gate: `createRootURLMiddleware` in `src/js/` on admin bundles needs per-hit justification | Admin-context React/JS bundles (`admin_enqueue_scripts`) | apifetch, middleware, wp-api-settings, redundancy, double-slash-404, silent-failure, admin-js | BUGS.md |
| B26 | Governance grep gates that hard-code a directory allow-list silently skip newly-added layers — enumerate every layer that participates in the rule (not just the layer named in the rule); validate every new gate against a KNOWN violation before shipping. F021 T118c scanned Controllers only + missed F024's admin/Partials/ `$wpdb` bypass. Fix: added T118d. Companion to B15 (regex form completeness) — same silent-pass symptom class | Verification gates, layering rules, code review | grep, gate-hygiene, layer-scope, silent-pass, boundary, refactor-hazard, verification-gate | BUGS.md |
| B27 | GitHub Actions matrix-cell check names are brittle for branch-protection `required_status_checks.contexts` — matrix cells register as separate names (`PHPUnit (pure) — PHP 8.1`) that shift when the matrix expands/contracts. Failure modes: new cell → silent coverage loss; removed cell → merges block on stale name; renamed job → all pins stale atomically. Prefer non-matrix single-job workflows for gate checks; matrix for coverage only; meta-job pattern if you must pin a matrix | CI configuration, branch protection | github-actions, matrix, required-status-checks, brittle-pinning, ci-drift, branch-protection | BUGS.md |
| B28 | Freemius auto-submenus require BOTH `menu.<key>` AND the corresponding top-level `has_<key>` / `is_<key>` in `fs_dynamic_init()`. `menu.addons` needs `has_addons`; `menu.pricing`/`menu.upgrade` need `has_paid_plans`; `menu.account` needs `is_registered() === true`. Symptom: silent no-render, zero log. Fix: grep SDK render gate (`class-freemius.php`) for the target key, add the second-level flag or expose it as an `AddonsPage` `$args` override (e.g. `fs_has_addons` in `acrossai-co/main-menu` 0.0.18). `menu.contact` and `menu.support` are the ONLY single-flag keys | Freemius integration, admin menu | freemius, two-level-enablement, menu-config, fs_dynamic_init, silent-no-render, generalizable | Superseded (F028) | BUGS.md |
| B29 | Vendor `add_action` inside `__construct` misses actions that fired before construct ran — when a vendor class is instantiated inside a later hook (e.g., our rest_api_init) and its ctor calls add_action for an earlier hook (e.g., wp_abilities_api_init), the listener attaches after the target action already fired. Symptom: `wp_get_ability()` blind to vendor-registered slugs at REST time. Fix: bypass runtime resolution and prefer plugin-owned canonical constants (F025 `ToolPolicy::PROTOCOL_TOOLS` + `PROTOCOL_TOOL_METADATA`) | Vendor integration, WordPress hook bootstrap | vendor-integration, add_action, hook-timing, silent-failure, canonical-constants, generalizable | Active (F025) | BUGS.md |
| B30 | Plugin composer shadowing a vendor discovery helper must mirror the vendor's type-filter semantic exactly, including `?? 'default'` fallbacks. F026 v1 missed the `$meta['mcp']['type'] ?? 'tool'` filter that vendor `DefaultServerFactory::discover_abilities_by_type()` applies → resource/prompt-typed public abilities leaked into `tools/list`, causing `tools/call` to 404. Prevention: read vendor filter chain end-to-end before shadowing; add per-type test cases that assert an ability of type X appears in ONLY the type-X composer output | Vendor integration, ability discovery composers | vendor-integration, type-filter, discovery-shadow, cross-type-leak, f026, mcp-adapter, generalizable | Active (F026) | BUGS.md |
| B31 | Plugin gates comparing `$tool_name` (as passed to `mcp_adapter_pre_tool_call`) against a bypass constant MUST account for vendor's `McpNameSanitizer::sanitize_name` which swaps `/` → `-` in ability slugs at MCP tool registration. Client-facing tool name = HYPHEN form; ability slug = SLASH form. Bypass constants listing only the slash form never match — gate rejects legitimate calls. F020's `EXCLUDED_SLUGS` had this bug for months, surfaced only when 070ffe2 made the three vendor meta tools first-class execution paths. Prevention: list BOTH forms in bypass constants; per-slug tests that call the gate with the sanitized form | Vendor integration, ability gate compares | vendor-integration, tool-name-sanitizer, bypass-constant, slash-hyphen, mcp-adapter, silent-failure, f020, generalizable | Active (F026 v3) | BUGS.md |
| B32 | Filter defaults that gate security or per-context enforcement decisions (visibility, exposure, ownership, capability) MUST be the canonical resolver's output — NEVER a partial derivation. Bug: `AbilityHelpers::apply_exposure_filter` defaulted to `meta.mcp.public` alone, silently ignoring F017's `is_exposed = 1` per-server overrides. Operator saw "1 of 6 abilities visible" because only the globally-public ability passed the shortcut default. Prevention: call the canonical resolver in the default (even when redundant-seeming); test the CANONICAL state end-to-end WITHOUT any test-registered filter callback intervening. General principle: filters let callers MUTATE the plugin's decision, not outsource it | Filter-default design | filter-default, canonical-resolver, exposure-resolver, dec-ability-override-resolution, apply-filters, security-adjacent, silent-failure, f017-f026, generalizable | Active (F026 v3) | BUGS.md |
| B33 | Admin gate keyed on a data field that a write path leaves empty by default silently falls open for every row created by that path. F024's per-connector settings gate resolved on empty `connector_slug` as "no settings apply", so DCR-registered clients (Claude.ai etc.) bypassed the operator's enable/disable toggle entirely. Prevention: every write path MUST populate every admin-gating field (or resolver MUST surface empty as admin-visible state, not fall open). Grep gate: any field added to an admin-gating resolver MUST show at least one populated write path per creation surface. F029 populates connector_slug at DCR registration time via first-matching-profile walk | Admin gating on data-fields | admin-gate, silent-bypass, data-model, connector_slug, f024, dcr, generalizable | Active (F029) | BUGS.md |
| B34 | Silent write-loss when a live BerlinDB table's schema drifts from Schema.php AND the stored `db_version` still matches code `$version`. INSERT of missing column returns `false`; caller casts to `int(0)` and treats as success. API returns 200 with data never persisted. Took down Claude OAuth on procureco.uk (`oauth_tokens`, live-fixed via DROP + recreate). Latent in `servers` (3 F025 `tool_*` columns) + `cli_auth_logs` (4 missing + 3 width-drifted) before F029. F011 phantom-version guard doesn't catch this — that only covers "physical table missing". Prevention: `D28` 3-part contract. Grep gate: any PR that changes `includes/Database/<Module>/Schema.php` MUST also touch the paired `Table.php` (`$version` + `$upgrades`). Sanity SQL for post-release audit: SELECT COLUMN_NAME, DATA_TYPE, CHARACTER_MAXIMUM_LENGTH FROM INFORMATION_SCHEMA.COLUMNS ... diff manually vs Schema.php | BerlinDB write path | berlindb, schema-drift, silent-write-loss, wpdb-insert-returns-false, generalizable | Active (F029) | BUGS.md |
| B35 | Filter-priority footrace on `wp_register_ability_args` — priority is a load-order coincidence, NOT a durable authorization boundary. Slot map: P10 = `CallbackReplacer` (3 vendor abilities), P100000 = sibling `acrossai-abilities-manager` per-slug injector, P999999 = F030 per-server operator-opt-in bypass. Any new consumer at OR ABOVE P999999 silently subsumes F030 semantics without any admin-visible signal. Prevention: before adding a `wp_register_ability_args` hook, check this slot map + update this entry + coordinate ordering in PR. If a 4th entrant emerges, consolidate into a `WpRegisterAbilityArgsPriorityMap` constants class (analog to `DEC-F020-TOOL-ENFORCEMENT-PRIORITY` for `mcp_adapter_pre_tool_call`). Accepted follow-up: boot-time detection at `plugins_loaded` P999999 to admin-notice conflicts — not shipped in F030. | `wp_register_ability_args` consumers, filter-priority coordination | filter-priority, footrace, wp-register-ability-args, priority-slot-map, callback-swap, generalizable | Active (F030) | BUGS.md |
| B36 | Inline `<script>` interpolation of dynamic PHP values into a JS string literal MUST use `wp_json_encode()`, NOT `esc_html()` / `esc_attr()`. Both are HTML-context escapers that leave `'` / `"` / `\` / `</script>` UN-escaped for JS parsers → stored XSS by any user who can set the interpolated value. `wp_json_encode()` produces a JSON-quoted string safe both as a JS literal AND as HTML content (encodes `<` as `<` to prevent `</script>` breakout); the emitted literal INCLUDES surrounding quotes so the `<script>` template must NOT wrap the placeholder in additional quotes. Grep gate: `grep -rn 'echo *"<script\|printf( *[\x27"]<script' includes/ admin/ public/ \| grep -v 'wp_json_encode'` — any hit needs `wp_json_encode`. Reference impl: `AccessControlTab.php:509-513` (SEC-030-001 remediation). Generalizable beyond F030 to any inline `<script>` emit path. | Any inline `<script>` emit path | inline-script, wp-json-encode, js-context-escaping, xss, esc-html-antipattern, sec-030-001, generalizable | Active (F030) | BUGS.md |

## Accepted Deviations
| ID | Deviation | Scope | Expiry/Review | Source |
|---|---|---|---|---|
| DEV1 | MCP Manager parent menu (`?page=acrossai_mcp_manager`) uses `WP_List_Table` instead of DataViews | Admin UI | Never expires — pre-approved exception | CONSTITUTION.md §IV |
| DEV2 | `includes/Compat.php` lives in `includes/` not `includes/Utilities/` — boot-time shim exception to Principle I | Boot flow | Review if Utilities/ autoload order changes | DECISIONS.md D3 |
| DEV3 | Bidirectional `FrontendAuth` ↔ `CliController` import accepted pending A9 promotion to `includes/Utilities/CliAuthRoutes.php` — `CliController::auth_start` reads `FrontendAuth::get_base_url`; `FrontendAuth::handle_*` reads `CliController::approve_auth_code` + `peek_pending_server` | Feature-007 cross-phase coupling | Resolve via tasks.md T044 in next hardening branch; constitution §I Modular Architecture (no new violations) | Feature-007 / 2026-06-30 architecture-review V2 |
| DEV4 | FR-029 shared parent menu bootstrap (`\AcrossAI_Main_Menu\SettingsPage`) lives in `acrossai-mcp-manager.php` plugin entry file on `plugins_loaded` priority 0 — scoped A1 exception for cross-plugin shared vendor-package resources; gated by `did_action('acrossai_main_menu_bootstrapped')` + `class_exists()` per D15. Companion FR-030 pre-activation vendor guard on `activate_<plugin>` priority 1. | Feature-010 cross-plugin coordination | Review only if `acrossai-co/main-menu` API changes shape OR if a third AcrossAI plugin creates a distinct shared-resource contract; scope permanent as long as the shared parent menu contract holds | Feature-010 / 2026-07-02 FR-031 + D15 |
| DEV5 | Per-server-edit tab sub-forms (`admin/Partials/ServerTabs/*::render_body()`) MAY use hand-rolled admin form HTML instead of `DataForm` from `@wordpress/dataviews` when the form has ≤ 3 configurable fields with no filter/sort/pagination requirement — DataForm's toolchain overhead (JS build entry + REST wiring) is disproportionate for single-field or small-field forms; shared WP admin form idioms (`wp_nonce_field`, `submit_button`, `checked()`, `esc_*`) already provide the §III security posture. Precedents (D13 escalation ≥ 2 features): Update Server tab (F013), Danger Zone tab (F013), Access Control per-server override toggle (F030). | Admin UI per-server-edit tabs | Never expires — pre-approved exception. Escalate to constitution amendment (add third §IV exception paragraph) if a 4th consumer emerges | Feature-013 (Update Server, Danger Zone) + Feature-030 (Access Control override); D13 escalation threshold |

## Security Constraints
| ID | Constraint | Scope | Tags | Source |
|---|---|---|---|---|
| S1 | All forms and AJAX endpoints MUST verify a nonce before processing data | Plugin-wide | nonce, security | CONSTITUTION.md §III |
| S2 | All REST routes MUST have an explicit `permission_callback` — `__return_true` only on public read routes | REST API | permissions | CONSTITUTION.md §III |
| S3 | OAuth tokens and Application Passwords MUST be stored hashed (SHA-256 min) — never plaintext | OAuth, Auth | secrets | CONSTITUTION.md §III |
| S4 | All DB queries MUST use `$wpdb->prepare()` | Database | sql-injection | CONSTITUTION.md §III |
| S5 | admin_url() MUST be wrapped with esc_url() before use in HTML — it is filterable via the admin_url hook | Admin UI | xss, escaping | PROJECT_CONTEXT.md |
| S6 | Singleton __construct() MUST be private — public constructor allows duplicate instantiation and double hook firing | Plugin-wide | singleton, security | PROJECT_CONTEXT.md |
| S7 | OAuth token endpoint is the documented exception to S2 — `__return_true` is allowed because RFC 6749 §2.3.1 specifies auth via POST body; exactly one match permitted across `includes/OAuth/`. **Post-F016 (2026-07-07): NO active consumers — `includes/OAuth/` directory retired entirely. Exception remains valid for any future OAuth token endpoint but currently has zero consumers. Any new `__return_true` REST route citing S7 as precedent MUST re-justify against RFC 6749 rather than referencing this row as active-in-use.** | REST API, OAuth | permission_callback, rfc-6749, exception, no-active-consumers-f016 | PROJECT_CONTEXT.md |
| S8 | Body-authenticated mutating REST routes broader than S7 — `__return_true` permitted on CLI device-code-grant flows when Content-Type allow-list rejects missing/unknown headers BEFORE field validation AND downstream credential is bound to consented resource scope | REST API, CLI auth | permission_callback, content-type, server-binding, exception | PROJECT_CONTEXT.md |
| S9 | Consent-surface displayed-state MUST be sourced from server-side authoritative store (transient/option/DB), not URL params — confused-deputy / UI-misrepresentation defense | Consent surfaces (CLI, OAuth, device-grant) | confused-deputy, ui-misrepresentation, consent, deep-link, deep-link-spoof | PROJECT_CONTEXT.md |

## Worklog Entries
| Date | Feature | Summary | Source |
|---|---|---|---|
| 2026-07-02 | F011 | BerlinDB-backed Table subclasses — phantom-version guard (`maybe_upgrade` override) prevents silent short-circuit when version option stamped but physical table missing | WORKLOG.md |
| 2026-07-03 | F012 | Vendor-owned shared Settings page — `register_setting()` option_group MUST be the shared `'acrossai-settings'` slug, not the per-tab page slug; wrong group makes Save silently no-op with no operator-visible error | WORKLOG.md |
| 2026-07-04 | F015 | Run `/speckit-analyze` AFTER `speckit-implement` when mid-session decisions supersede spec (Q4/Q1 pivots, user-directed table rename, technology pivots) — catches stale FR text, dead-code follow-ups, docs-vs-shipping drift the pre-implement audit can't see | WORKLOG.md |
| 2026-07-07 | F016 | Fresh-install-only retirement — when operator attestation covers no-live-data, retirement features MUST NOT ship in-plugin schema-migration code; delegate DROP/ALTER to operator via README recipe. Removed ~4,000 LOC + ~80 LOC of migration helpers (relative to initial plan). See D21 | WORKLOG.md |
| 2026-07-07 | F017 | Per-server ability selection — durable lessons: (1) per-resource overrides that fall back to a global default belong in a single shared resolver, not duplicated across every consumer (DEC-ABILITY-OVERRIDE-RESOLUTION); (2) call-time enforcement of stored per-server overrides rides the same `mcp_adapter_pre_tool_call` shape as F015 access control (D18); wire at priority 20 so a hidden verdict supersedes any earlier allow. SEC-001 plan-review HIGH finding surfaced the storage-vs-enforcement decoupling gap mid-flow — closed via Q4 clarification + FR-030 + TASK-10 without re-plan | WORKLOG.md |
| 2026-07-09 | F020 | Per-server tool selection — two workflow lessons: (1) **defer UX-facing ADR capture** until AFTER `/speckit-security-review-staged` runs cleanly, not pre-plan them speculatively — F020's planned `DEC-TOOL-EDIT-EXPLICIT-BATCH-COMMIT` (Save/Cancel) was withdrawn post-implement when the operator asked to remove Save/Cancel in favor of optimistic-per-toggle; (2) **run security-review v2 for close-in-substance verification** — SEC-020-007 vendor-accessor bug (`B24`) survived plan-review v1's remediation because v1 didn't verify contract text against vendor source; v2 caught it. Also codified `DEC-TOOL-SELECTION-PRESENCE-MODEL` (presence-based storage over boolean when UX has no third state) and `DEC-F020-TOOL-ENFORCEMENT-PRIORITY` (priority-30 slot on `mcp_adapter_pre_tool_call` annotating D18) | WORKLOG.md |
| 2026-07-12 | F021 | Governance wrap-up: D13 applied for the 3rd time (constitution v1.0.0 → v1.1.0 adds narrowly-scoped "Connector picker card layout" exception to Principle IV, retiring the soft-deviation-per-feature ceremony); B26 caught + remediated (T118c scanned Controllers only → added T118d for admin/Partials; moved F024 DB access behind Repository methods); D22 codified (F024 folded into F021 tasks.md as 32 concrete T024-* IDs instead of "See F###" pointer); branch protection enabled on `main` with 6 non-matrix required checks (PHPUnit matrix intentionally omitted per B27) + CODEOWNERS added | WORKLOG.md |
| 2026-07-13 | F022 | Shared Add-ons submenu + umbrella-product Freemius model — 4 upstream vendor releases (main-menu 0.0.15 → 0.0.18) landed in one branch under phases 4b/4c/4d/4e with concrete T-blocks per phase. Established canonical shape for iterating an owned vendor package inside a single spec-kit feature: each release cites its upstream commit SHA + tag in the tasks.md phase that consumed it; PR-side commits reference the phase they closed. Also caught + remediated a CRITICAL duplicate-task-ID mistake (Phase 5 T030-T033 collided with Phase 4c; Phase 6 T040-T046 collided with Phase 4e) via Phase 7 T070 fold-in — non-overlapping T-block ranges are now the rule. Companion invariants: A16 (composer VCS entry), B28 (Freemius two-level menu-config), DEC-ADDONS-PAGE-VENDOR-CTOR-BOOT, DEC-FREEMIUS-DOUBLE-OPTIN-GATES-ACCOUNT | WORKLOG.md |
| 2026-07-14 | F025 | Server tools registration hooks — hybrid DB-authoritative tool storage (boolean columns + presence rows) with operator-removable protocol tools via ConfirmDialog + companion-plugin filter + vendor filter callback. Durable lessons: hybrid storage pattern (DEC-F025-HYBRID-TOOL-STORAGE-PROTOCOL-VS-CURATED), vendor cross-check cadence + runtime-evidence-over-static-review (DEC-F025-V2-VENDOR-SOURCE-CROSS-CHECK-CADENCE), task-review preservation invariants + coverage matrices (DEC-F025-TASKS-REVIEW-PRESERVATION-INVARIANT-AND-COVERAGE-MATRIX), B29 vendor add_action-inside-construct hook-timing bug (fixed by FR-018 validation bypass + PROTOCOL_TOOL_METADATA GET catalog fallback). Ran 5 security-review passes: plan v1 (3 INFO), plan v2 (1 LOW + 2 INFO), tasks (3 INFO), architecture (0), staged (2 INFO) — all remediated | WORKLOG.md |
| 2026-07-17 | F028 | Retire Freemius integration; consume main-menu 0.0.22 filter-driven Add-ons page. Vendor 0.0.22 drops `freemius/wordpress-sdk` + `AcrossAI_Addon\` namespace; plugin deletes 94-line AddonsPage instantiation block and ships new `Admin\Partials\AddonsFilter` singleton that hooks `acrossai_addons` to drop own slug (consumer self-exclusion pattern — an active plugin should not advertise itself as installable). Related retired: DEC-ADDONS-PAGE-VENDOR-CTOR-BOOT, DEC-FREEMIUS-DOUBLE-OPTIN-GATES-ACCOUNT, B28 — flipped to Superseded (F028) | WORKLOG.md |
| 2026-07-18 | F029 | OAuth /token accepts HTTP Basic auth (RFC 6749 §2.3.1) with CGI fallback + `client_secret_post` softening (fall through to PKCE-only when no secret submitted) + DCR clients attributed to connector profiles at registration time (fixes F024 gate silent-bypass) + BerlinDB schema-drift reconciliation for `cli_auth_logs` (type mismatches) and `servers` (3 F025 `tool_*` columns) via `$upgrades` callbacks + new `Main::reconcile_database_schemas()` on `admin_init@3`. Codifies D27 (soft-auth via PKCE), D28 (BerlinDB $upgrades reconciliation pattern), B33 (admin-gate-on-data-field bypass), B34 (silent write-loss on schema drift). Runtime-side complement to F027 (v0.1.2 DCR default). | WORKLOG.md |
| 2026-07-20 | F030 | Per-server ability `permission_callback` override — operator toggle on the Access Control tab that, when ON, causes exposed abilities to bypass their `permission_callback` for MCP requests routed to THIS server. Codifies D29 (six-layer defensive gating framework: cap + nonce + banner + confirm + `CurrentServerHolder` scope + `ExposureResolver` gate; scoped carve-out from D24), D30 (empty-`$meta` to `ExposureResolver::resolve()` — explicit-junction-row-only bypass; scoped carve-out from `DEC-ABILITY-OVERRIDE-RESOLUTION`), B35 (`wp_register_ability_args` priority slot map: P10 CallbackReplacer / P100000 sibling / P999999 F030), B36 (inline `<script>` interpolation requires `wp_json_encode()` — SEC-030-001 remediation; generalizable beyond F030), DEV5 (per-server-edit tab hand-rolled form exception to §IV DataForm mandate per D13 escalation ≥ 2 features). Also patched pre-existing test-infrastructure gap: `abilities` / `database` / `mcp` PHPUnit suites added (F011/F017/F026 legacy tests were orphaned; CI never ran them). | WORKLOG.md |

## Security Reviews
| File | Phase | Date | Risk | Findings | Constraints |
|---|---|---|---|---|---|
| docs/security-reviews/2026-07-02-011-berlindb-migration-plan.md | plan | 2026-07-02 | LOW | C:0 H:0 M:0 L:3 | A02,A04,A05,A08,A09 |
| docs/security-reviews/2026-07-03-012-mcp-settings-tab-plan.md | plan | 2026-07-03 | LOW | C:0 H:0 M:0 L:3 | A02,A05,A08,A09 |
| docs/security-reviews/2026-07-03-013-per-server-tabs-refactor-plan.md | plan | 2026-07-03 | LOW | C:0 H:0 M:0 L:3 | A01,A03,A05,A09 |
| docs/security-reviews/2026-07-04-015-access-control-v2-adoption-plan.md | plan | 2026-07-04 | LOW | C:0 H:0 M:0 L:3 | A01,A03,A05,A09 |
| docs/security-reviews/2026-07-07-017-per-server-ability-selection-plan.md | plan | 2026-07-07 | HIGH | C:0 H:1 M:1 L:2 | A01,A03,A05,A09 |
| docs/security-reviews/2026-07-09-020-per-server-tool-selection-plan.md | plan | 2026-07-09 | HIGH | C:0 H:1 M:1 L:2 | A01,A04,A05,A08 |
| docs/security-reviews/2026-07-09-020-per-server-tool-selection-plan-v2.md | plan | 2026-07-09 | HIGH | C:0 H:1 M:0 L:2 | A01,A05,A09 |
| docs/security-reviews/2026-07-09-020-per-server-tool-selection-tasks.md | tasks | 2026-07-09 | LOW | C:0 H:0 M:0 L:3 | A04,A05,A09 |
| docs/security-reviews/2026-07-09-020-per-server-tool-selection-staged.md | staged | 2026-07-09 | INFORMATIONAL | C:0 H:0 M:0 L:0 | A05 |
| docs/security-reviews/2026-07-13-025-server-tools-registration-hooks-plan.md | plan | 2026-07-13 | LOW | C:0 H:0 M:0 L:0 I:3 | A01,A04,A08 |
| docs/security-reviews/2026-07-13-025-server-tools-registration-hooks-plan-v2.md | plan | 2026-07-13 | LOW | C:0 H:0 M:0 L:1 I:2 | A04,A08 |
| docs/security-reviews/2026-07-14-025-server-tools-registration-hooks-tasks.md | tasks | 2026-07-14 | LOW | C:0 H:0 M:0 L:0 I:3 | A04,A05 |
| docs/security-reviews/2026-07-14-025-server-tools-registration-hooks-staged.md | staged | 2026-07-14 | LOW | C:0 H:0 M:0 L:0 I:2 | A04 |
